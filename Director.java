package Modelo;

public class Director extends Empleado {
    public Director(String nombre, String curp, String correo, String telefono, String noPersonal, String fechaIngreso) {
        super(nombre, curp, correo, telefono, noPersonal, fechaIngreso);
    }
}